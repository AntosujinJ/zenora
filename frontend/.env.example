# Local dev: leave this UNSET — the Vite proxy sends /api to http://localhost:8000.
# Production (Vercel): set to your deployed backend URL, e.g.
# VITE_API_URL=https://zenora-api.onrender.com
VITE_API_URL=
